export const DEFAULT_HISTORY_SIZE = 50;
export const GLOBAL_SHORTCUT = 'CommandOrControl+Shift+B';
export const ACTIONS_SHORTCUT = 'CommandOrControl+Shift+Space';
export const POLL_INTERVAL_MS = 500;
/** Max gap between two solo taps of the modifier key to count as a double-tap. */
export const DOUBLE_TAP_WINDOW_MS = 400;
/** Max time the modifier key can be held for a single press to still count as a "tap". */
export const DOUBLE_TAP_MAX_HOLD_MS = 350;
